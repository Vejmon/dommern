x-app: &baseapp
  platform: linux/arm64
  restart: unless-stopped
  network_mode: host
  depends_on:
    dommern-db:
      condition: service_healthy
  environment:
    SPRING_PROFILES_ACTIVE: production
    SPRING_MAIL_USERNAME: ${EMAIL-USER}
    SPRING_MAIL_PASSWORD: ${EMAIL-PW}
# trigg

services:
  app:
    <<: *baseapp
    container_name: app
    build: .

  dommern:
    <<: *baseapp
    container_name: dommern
    image: ghcr.io/vejmon/dommern:latest

  db: &basedb
    container_name: dommern-db
    image: 'postgres:17.4'
    environment:
      - 'POSTGRES_DB=dommern'
      - 'POSTGRES_PASSWORD=password'
      - 'POSTGRES_USER=user'
    network_mode: host
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: [ "CMD-SHELL", "sh -c 'pg_isready --port 5432 --username=user --dbname=dommern'" ]
      interval: 2s
      retries: 5
      timeout: 1s

  dommern-db:
    <<: *basedb
    platform: linux/arm64
    volumes:
      - ./pgdata:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql