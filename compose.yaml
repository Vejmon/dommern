services:
  dommern-db:
    container_name: dommern-db
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=race'
      - 'POSTGRES_PASSWORD=user'
      - 'POSTGRES_USER=password'
    ports:
      - '5432'
    networks:
      - api

networks:
  api:
    name: api
    driver: bridge

