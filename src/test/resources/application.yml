spring:
  config:
    activate:
      on-profile: test
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql:17.4:///dommern
    username: user
    password: password
  flyway:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql:17.4:///dommern
    user: flyway
    password: password